import { generateText, generateObject } from 'ai';
import { z } from 'zod';
import { google } from "@ai-sdk/google"

async function main() {

}

main().catch((error) => {
    console.error("Error:", error);
    process.exit(1);
});
