[package]
name = "starina"
publish = false
version = { workspace = true }
authors = { workspace = true }
edition = { workspace = true }

[lib]
path = "lib.rs"

[features]
default = ["userspace"]
in-kernel = []
userspace = []
